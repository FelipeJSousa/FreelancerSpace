@model IEnumerable<FreelancerSpace.Models.GrupoAcessoModel>

@{
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Grupo Acessos</title>
</head>
<body>
<p>
    <a asp-action="Create">Criar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                @Html.ActionLink("Editar", "Create", "GrupoAcessos", new { id = item.Id }) |
                @Html.ActionLink("Excluir", "Excluir", "GrupoAcessos", new { id = item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
